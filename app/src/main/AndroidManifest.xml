<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apirest">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:noHistory="true"
            android:name=".Activities.MainActivity"
            android:label="LISTA PROSPECTOS"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:noHistory="true"
            android:name=".Activities.PersonaActivity"
            android:label="EDITAR PROSPECTO">
        </activity>
        <activity
            android:name=".Activities.AgregarPersona"
            android:label="AGREGAR PROSPECTO">
        </activity>
        <activity
            android:name=".Activities.VerDocumentos"
            android:label="VER DOCUMENTOS ">
        </activity>

    </application>

</manifest>